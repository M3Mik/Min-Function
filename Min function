def min_value(numbers):
    min_num = numbers[0]
    for num in numbers:
        if num < min_num:
            min_num = num
    return min_num
Input_numbers = input().split()
Input_numbers = [int(x) for x in Input_numbers]
print(min_value(Input_numbers))